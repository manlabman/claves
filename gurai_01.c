#include<iostream>
#include<libstd.h>
#include<pic 16F887A>

int main (){
    in Total=100
    int *pul = k;
    float Sum=incc[i];
    Porcc=sum

    char *p=files;
    for(i=0;i<100;ii++);
    for(j=0;j<8;j++);
    
    iff(i=0){
        j[]=
    
    printf(pulsarr el primer boton\n;&b);
    if(b1==100%);
    break;
    activar buzz
    if(b1,)


    return 0;

}